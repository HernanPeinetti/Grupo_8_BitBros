<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head', {titulo: 'Iniciar Sesión' , estilo: 'login' }) %>

  <body>
    <%- include('../partials/header-user') %>
      <main>
        <div class="container">
          <h2>Te damos la bienvenida</h2>
          <% if (locals && locals.errors) { %>
            <% for(let error in errors) { %>
              <p>
                <%= errors[error].msg %>
              </p>
              <% } %>
                <% } %>
          <form action="/login" method="post">
            
            <div class="containerEmail">
              <label for="email">Correo Electrónico:</label>
              <input type="email" value="<%= locals.old ? old.email : ''  %>" id="email" name="email" >
            </div>

            <div class="containerContraseña">
              <label for="password">Contraseña:</label>
              <input type="password" id="password" name="password" >
            </div>


            <button type="submit">Iniciar Sesión</button>

            <label class="checkRemember" name ="recordarUser">
              <input type="checkbox" class="check" name="recordarUser">Recordar Usuario
            </label>
          </form>

          <p class="forgot-password"><a href="#">¿Olvidaste tu contraseña?</a></p>

          <div class="social-buttons">
            <button class="facebook">Continuar con Facebook</button>
            <button class="google">Continuar con Google</button>
          </div>

          <p class="register-link">¿Aún no tienes una cuenta? <a href="/register">Regístrate</a></p>
        </div>
      </main>

  </body>

</html>