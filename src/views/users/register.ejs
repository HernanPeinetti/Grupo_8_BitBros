<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head', {titulo: 'Registro Isabel Bicicleteria' , estilo: 'register' }) %>

  <body>
    <%- include('../partials/header-user') %>
      <main>
        <div class="container">
          <h2>Te damos la bienvenida a Isabel Bicicleteria</h2>
          <% if (locals && locals.errors) { %>
            <% for(let error in errors) { %>
              <p>
                <%= errors[error].msg %>
              </p>
              <% } %>
                <% } %>
                  <form action="/register" method="POST" enctype="multipart/form-data">
                    <label for="name">Nombre y Apellido:</label>
                    <input type="text" value="<%= locals.old ? old.name : ''  %>" name="name"
                      placeholder="Por ej: Juan Perez">

                    <label for="birth">Fecha de Nacimiento:</label>
                    <input type="date" value="<%= locals.old ? old.birth : ''  %>" name="birth">

                    <label for="email">Correo Electrónico:</label>
                    <input type="email" value="<%= locals.old ? old.email : ''  %>" name="email">


                    <label for="password">Contraseña:</label>
                    <input type="password" id="password" name="password">

                    <label for="avatar">Foto de perfil:</label>
                    <input type="file" name="avatar">
                    <div class="checkbox-container">
                      
                      <label for="recordarme" class="checkbox-label">Recordarme</label>
                      <input type="checkbox" id="recordarme" class="custom-checkbox">
                  </div>
                    <input type="submit">Continuar</input>

                  </form>

                  <div class="social-buttons">
                    <button class="facebook">Iniciar sesión con Facebook</button>
                    <button class="google">Iniciar sesión con Google</button>
                  </div>

                  <p class="login-link">¿Ya tienes cuenta? <a href="/login">Iniciar Sesión</a></p>
        </div>
      </main>

  </body>

</html>